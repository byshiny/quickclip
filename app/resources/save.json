{
  "comment": "I'm trying to keep format consistent, so I'm going to have",
  "circularBuffer": [
    "\"comment\": \"To specify a new shortcut, you need to obtain the keycode,\n  \\then put the keycode corresponding to the pasteBuffer after naming\n  the string pasteBuffer + p and specify a display value, which will show\n  up on when you press cmd + opt \",",
    "strippedKey",
    "keyValue",
    "strippedKey",
    "strippedKey",
    "var strippedKey = key.replace('pasteBuffer','');",
    "keyvalue",
    "shortcutKeys",
    "buffalse",
    "shortcutKeys"
  ],
  "keyBuffer": {
    "1": "lobalShortcut.unregisterAll()",
    "2": "clipboard.writeText(currentText)",
    "3": "=(",
    "4": "=(",
    "5": "=(",
    ";": "=(",
    "'": "=(",
    "{": "lobalShortcut.unregisterAll()",
    "copyKey": "=(",
    "showKey": "=(",
    "circBufferKey": "=(",
    "Semi": "=(",
    "Quotes": "=(",
    "Bracket": "=("
  }
}